package com.lbo.mobile.laybare.shared.util

import com.lbo.mobile.laybare.BuildConfig
import com.lbo.mobile.laybare.R

object Constants {
    val BASE_URL = "https://booking.lay-bare.com/"

    val SPLASH_VIDEO = "android.resource://" + BuildConfig.APPLICATION_ID + "/" + R.raw.splash

}